package OMTPermissions;

import java.security.Principal;

import org.jboss.seam.security.permission.PermissionCheck;
import org.jboss.seam.security.Role;

rule "CanUserUploadMidi"
when
  c: PermissionCheck(name == "midiUpload")
  Role(name == "admin")
then
  c.grant();
end 